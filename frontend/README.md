# Glow Products — storefront

React single-page storefront for the hybrid recommender. Create React App;
calls the Flask API at `http://localhost:5001`.

```bash
npm install        # first time only
npm start          # -> http://localhost:3000
```

See `../RUNNING.md` for the full stack (database provisioning, backend, and
the offline experiment pipeline).
